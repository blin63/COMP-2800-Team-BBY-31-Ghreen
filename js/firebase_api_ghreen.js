  // Ghreen's Firebase configuration
  // Contact Ghreen developers for api key to make app fully functional
  var firebaseConfig = {
    
  };

  // Initialize Firebase
  firebase.initializeApp(firebaseConfig);
  const db = firebase.firestore();